---
'kahraman': minor
---

Initial release: accessibility-first, codecept-style test actor (`createActor`) and locator DSL (`role`, `text`, `heading`, `button`, `link`) for Storybook portable stories and Vitest browser mode, plus an opt-in `kahraman/preview` diagnostics annotation.
